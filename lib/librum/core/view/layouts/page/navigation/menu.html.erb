<div class="navbar-menu">
  <div class="navbar-start">
    <% items.select(&:left?).each do |item| %>
    <%=
      render(
        Librum::Core::View::Layouts::Page::Navigation::Item.new(item: item)
      )
    %>
    <% end %>
  </div>

  <div class="navbar-end">
    <% items.select(&:right?).each do |item| %>
    <%=
      render(
        Librum::Core::View::Layouts::Page::Navigation::Item.new(item: item)
      )
    %>
    <% end %>
  </div>
</div>
