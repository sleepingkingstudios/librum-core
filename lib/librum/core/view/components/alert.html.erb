<div class="<%= class_names %>">
  <% if dismissable? %>
  <button class="delete is-medium"></button>
  <% end %>

  <% if render_icon? %>
  <%=
    render(
      Librum::Core::View::Components::IconText.new(
        contents: message,
        icon:     icon
      )
    )
  %>
  <% else %>
  <%= message %>
  <% end %>
</div>
